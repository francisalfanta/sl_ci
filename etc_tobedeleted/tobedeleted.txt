form = dialog.find("form").on("submit", function(event) {
 event.preventDefault();
addUser(); <--ajax instead
});
how to get id value for eACH record?
 answer: 
  1.   $(#create-user").button().on("click", function(){
	  dialog.dialog("open");
      });
  2. $("#create-user").click(function(e){
		e.preventDefault();
		var url = $(this).attr("href");
        var qryString = $(this).attr("id");
        var fullUrl = url+'?id='qryString;
